apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mariadb.fullname" . }}-db-auth
  namespace: {{ .Release.Namespace | quote }}
type: Opaque
data:
  MYSQL_PASSWORD: {{ default b64enc "secret" tpl .Values.global.dbPassword . | b64enc }}
  MYSQL_ROOT_PASSWORD: {{ default b64enc "secret" tpl .Values.global.dbPassword . | b64enc }}
